export interface ArticleInterface {
    body:string,
    createdAt:string,
    description:string,
    favorited:boolean,
    favoritesCount:boolean,
    slug:string,
    tagList:string[],
    title:string,
    updatedAt:string

    // TODO: Add author interface 
}